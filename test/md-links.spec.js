// const mdLinks = require('../');
import { mdLinks } from '../index.js';

describe('mdLinks', () => {
  it('is a function', () => {
    expect(typeof mdLinks).toBe('function');
  });
})

describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

});

